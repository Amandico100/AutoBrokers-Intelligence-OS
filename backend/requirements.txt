# FastAPI Core
fastapi>=0.109.0
uvicorn[standard]==0.27.0
pydantic==2.12.4
pydantic-settings==2.12.0

# LangChain & LangGraph — SPEC-116 F2 (§8, D-116-08): sobe SÓ o que tem necessidade
# concreta. langchain-anthropic 1.7.3 = effort nativo do Claude (output_config.effort);
# langchain-openai 1.6.4 = Responses API p/ GPT-6 com tools+raciocínio; langchain-core 1.6.4
# é exigido pelas duas; langgraph 1.2.x exige langgraph-checkpoint >= 4.1 (fecha
# CVE-2026-27794 / CVE-2026-48775). langchain 1.0.8 NÃO resolve com langgraph 1.2 (📊 pip
# --dry-run py3.11 manylinux, 23/09/2026) → 1.4.2 junto, com community/experimental/splitters.
langchain==1.4.2
langchain-anthropic==1.7.3
langchain-community==0.4.2
langchain-core==1.6.4
langchain-experimental==0.4.2
langchain-google-genai==3.1.0
langchain-openai==1.6.4
langchain-text-splitters==1.1.2
langgraph==1.2.12
langgraph-checkpoint==4.2.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.5

# LLM Providers — pin EXATO (build reprodutível, F10). ⛔ NÃO subir openai 3.x nem
# anthropic 1.x: trocam httpx por httpx2 e o relógio classifica erro por httpx
# (relogio_do_modelo.e_transitorio). Sem necessidade concreta hoje (SPEC-116 §6 "Sai").
openai==2.54.0
anthropic==0.125.0
google-ai-generativelanguage==0.9.0

# Cryptography
cryptography>=42.0.0
bcrypt>=4.0.0

# Database & Storage
supabase==2.10.0
psycopg[binary]>=3.0.0
psycopg-pool>=3.2.0
langgraph-checkpoint-postgres==3.1.2

# Vector Store
qdrant-client>=1.11.0

# Object Storage
minio==7.2.3

# Document Processing
PyPDF2==3.0.1
# PyMuPDF le condicao geral preservando a ESTRUTURA da pagina. 📊 Medido em
# 08/08/2026 no mesmo PDF (Porto CG140): PyPDF2 devolve 519 linhas e 3
# ocorrencias de `CLAUSULA N` em inicio de linha; PyMuPDF devolve 11.064 linhas
# e 74 — e em 1,3s contra 15,6s. PyPDF2 colapsa a pagina inteira numa linha so,
# e sem linha nao ha titulo, nem secao, nem corte por secao.
PyMuPDF==1.28.0
python-docx==1.1.0

# Utils
httpx==0.27.2  # supabase 2.10.0 exige httpx<0.28 (📊 pip --dry-run 23/09/2026)
requests>=2.31.0
python-dotenv==1.0.1
python-multipart==0.0.6
pytz>=2024.1

# Evaluation
pandas>=2.0.0
tqdm>=4.66.0

# Reranking
cohere>=5.0.0

# Hybrid Search
fastembed>=0.2.0

# Web Search
tavily-python>=0.3.0

# Message Buffer
redis>=5.0.1
apscheduler>=3.10.4
python-slugify>=8.0.0

# Background Workers (Billing)
celery[redis]>=5.3.0

# Retry Logic
tenacity>=8.2.0

# Observability (LangSmith)
langsmith>=0.1.0

# Cache Management
cachetools>=5.3.0

# Rate Limiting
slowapi==0.1.9

sentry-sdk[fastapi]

# Payments
stripe>=7.0.0

# Email
sendgrid>=6.10.0

# PII & Safety
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.0.0
groq>=0.4.0

# Server
gunicorn>=21.2.0

# Code Quality
ruff>=0.14.0