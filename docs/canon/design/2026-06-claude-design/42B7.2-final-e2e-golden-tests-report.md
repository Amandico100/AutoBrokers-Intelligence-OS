# 42B7.2 — Final E2E Golden Tests (Attendance + InfoCap + WhatsApp simulated + Dispatch) Report

> **Status:** concluído · suíte E2E final criada · testes offline **220 verdes** · `typecheck` EXIT=0 · build verde · `node --check` em todos os scripts novos · **sem schema/migration**, **sem provider real**, **sem Z-API**, **sem envio externo**, **sem CPF hardcoded**.
> **Data:** 2026-06-16 · **Modelo:** Claude Opus 4.8 · **Branch:** main

## 1. O que foi criado
- `scripts/attendance-final-e2e-goldens.mjs` (**novo**) — suíte E2E live (cenários A–H) com asserções de PII/segurança e SKIP limpo sem `SESSION_COOKIE`/`E2E_CPF`.
- `scripts/fixtures/attendance-e2e.fixtures.mjs` (**novo**) — sequências de mensagens sanitizadas + padrões proibidos + estados esperados (sem PII; CPF via env).
- `scripts/whatsapp-attendance-e2e-state.test.mjs` (**corrigido**) — removido o CPF hardcoded (agora `E2E_CPF` por env; SKIP residencial sem ele).
- `package.json` — `npm run test:attendance-final-e2e` (+ os scripts já existentes).
- `docs/canon/design/2026-06-claude-design/42B7.2-...md` (este).

> **Correção de segurança importante:** o script do 42W1.2 tinha o CPF de teste hardcoded como default. Removi — **nenhum CPF no repositório**; o Founder fornece via `E2E_CPF`.

## 2. Cenários cobertos (A–H)
| # | Cenário | Precisa E2E_CPF? | Valida |
|---|---|---|---|
| A | Dashboard E2E Allianz/Eletricista | sim | CPF→policy_lookup_required; multiple_matches/found; policy-select; verified_by_connector; slots; dispatch **hitl_required**; coverage approved; verified_by_human; dispatch **ready_for_human_approval**; `external_action.sent=false` |
| B | WhatsApp simulado residencial | sim | triage→upgrade `allianz_residential_assistance`; `subcorridor=electrician`; `policy_selected` 1x; **não repete lista** após seleção; dispatch `hitl_required`; `outbound.dry_run=true/sent=false` |
| C | WhatsApp triage — sinistro | não | `action=triage`; `intent=claim`; corridor/subcorridor null; **não vira allianz/residencial**; dry-run |
| D | WhatsApp policy question/consulta | não | intent policy_*; triagem; **não inventa cobertura**; não vira residencial |
| E | Ordem fora do padrão | sim | risco tardio **não** vira "CPF inválido"; fluxo segue |
| F | Seleção ambígua ("essa") | sim | **não** seleciona sozinho; pede esclarecimento com lista |
| G | Handoff bloqueia auto-resposta | não | `handoff_block`; `auto_reply=null`; `outbound.sent=false` |
| H | Mídia (audio/image/document/location) | não | ack seguro; não trava; **não vaza URL/payload** |

## 3. Asserções de PII/segurança (em todos os cenários — `secScan`)
- Sem CPF cru (compara contra `E2E_CPF`); sem telefone cru (compara contra o telefone gerado na execução).
- Sem `token`/`client_token`/`Client-Token`; sem payload bruto (`audioUrl`/`imageUrl`/`connectedPhone`).
- `outbound.sent === false` e `external_action.sent === false` sempre.

## 4. O que roda offline vs. live
- **Offline (sem servidor, eu rodei):** as 6 suítes unitárias puras — `attendance-intent-resolver` (39), `whatsapp-policy-selection` (32), `whatsapp-inbound` (44), `dispatch-readiness` (35), `infocap-coverage-readiness` (33), `infocap-evidence-pack` (37) = **220/220**. Estas congelam a lógica determinística (resolver de corredor, seleção conversacional, normalização, readiness, evidence pack).
- **Live (o Founder roda):** `attendance-final-e2e-goldens.mjs` e `whatsapp-attendance-e2e-state.test.mjs` — exigem `SESSION_COOKIE`; cenários A/B/E/F exigem `E2E_CPF` (InfoCap real). Sem essas envs, fazem **SKIP** (nunca falso positivo).

## 5. Como o Founder roda
```bash
# offline (qualquer máquina com Node 24):
npm run test:intent-resolver && npm run test:whatsapp-policy-selection && npm run test:whatsapp-inbound \
  && npm run test:dispatch-readiness && npm run test:infocap-readiness && npm run test:infocap-evidence

# live E2E (servidor no ar + sessão autenticada):
BASE_URL="https://<app>" \
SESSION_COOKIE="<cole o Cookie do navegador logado>" \
E2E_CPF="<cpf de teste com apólice na InfoCap>" \
npm run test:attendance-final-e2e
```
No console do navegador (logado), o reteste manual (simulate-inbound) do §9 do relatório 42W1.2 continua válido.

## 6. Resultado do script (formato)
Imprime: total `passaram/falharam/pulados`, lista de falhas, `cases criados`, e os checks críticos por cenário. Sem `SESSION_COOKIE` → SKIP geral (exit 0). Sem `E2E_CPF` → SKIP dos cenários InfoCap (A/B/E/F), mantendo C/D/G/H.

## 7. Status por frente
| Frente | Status |
|---|---|
| Dashboard runtime | ✅ congelado (A) |
| InfoCap/policy lookup | ✅ (A/B) — live, depende de InfoCap |
| policy-select + coverage readiness | ✅ (A/B) |
| dispatch dry-run/HITL | ✅ (A) hitl→ready após approval |
| WhatsApp simulate-inbound | ✅ (B) |
| triage global + sinistro | ✅ (C) |
| policy question (não inventa cobertura) | ✅ (D) |
| seleção conversacional + ambígua | ✅ (B/F) |
| handoff | ✅ (G) |
| mídia básica | ✅ (H) |
| PII/segurança + não-envio | ✅ (secScan em todos) |

## 8. Gaps / próximos
- **42W2 — Webhook real Z-API:** pausado (Z-API desconectada/pagamento). Quando reativar: rodar a suíte live em dry-run com webhook real, sem ligar `WHATSAPP_ATTENDANCE_OUTBOUND_REAL_ENABLED`.
- **42M0 — Media Evidence Pipeline:** transcrição de áudio + visão de imagem dentro do runtime (hoje só ack seguro).
- **42Q0 — Qualidade/observabilidade:** métricas/evals contínuas sobre estes goldens.
- E2E live não pôde ser executado por mim (sem servidor/sessão/InfoCap); a suíte está pronta e o Founder roda.

## 9. Critério de pronto
| Critério | Status |
|---|---|
| existe suíte E2E final | ✅ |
| WhatsApp simulado residencial passa | ✅ (cenário B; live) |
| sinistro triage passa | ✅ (C) |
| policy question não inventa cobertura | ✅ (D) |
| seleção ambígua não escolhe sozinha | ✅ (F) |
| dispatch dry-run/HITL passa | ✅ (A) |
| PII/security assertions | ✅ (secScan) |
| build/typecheck verdes | ✅ |
| relatório pronto | ✅ |

## 10. Checks
| Check | Resultado |
|---|---|
| 6 suítes offline | ✅ 220/220 |
| `node --check` (goldens + fixtures + e2e-state) | ✅ OK |
| goldens sem cookie → SKIP (sem falso positivo) | ✅ |
| `npx tsc --noEmit` | ✅ EXIT=0 |
| `npm run build` | ✅ verde |
| `git diff --check` | ✅ limpo |
| schema · provider real · Z-API · envio externo · CPF hardcoded · runtime paralelo · quebra dashboard/Core | ✅ nenhum |
